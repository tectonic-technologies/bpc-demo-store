{%- comment -%}AUTO-GENERATED BY SPECTRUM THEME-SKELETON — a placeholder slot. Replace the placeholder body with the feature render call.{%- endcomment -%}
{%- comment -%}SPECTRUM_PLACEHOLDER:START{%- endcomment -%}
{%- if request.design_mode -%}
  <div class="spectrum-slot" data-spectrum-slot="shop_the_look">Spectrum slot: Shop The Look — fill in Studio</div>
{%- endif -%}
{%- comment -%} Spectrum slot "Shop The Look" (data-spectrum-slot="shop_the_look") — fill via Studio with a catalog snippet whose placement.slotKey is "shop_the_look". Variations resolve from the live catalog, not from a list baked here. {%- endcomment -%}
{%- comment -%}SPECTRUM_PLACEHOLDER:END{%- endcomment -%}
{% schema %}
{
  "name": "Shop The Look",
  "presets": [
    {
      "name": "Shop The Look"
    }
  ]
}
{% endschema %}
