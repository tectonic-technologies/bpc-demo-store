{%- comment -%}AUTO-GENERATED BY SPECTRUM THEME-SKELETON — a placeholder slot. Replace the placeholder body with the feature render call.{%- endcomment -%}
{%- comment -%}SPECTRUM_PLACEHOLDER:START{%- endcomment -%}
{%- if request.design_mode -%}
  <div class="spectrum-slot" data-spectrum-slot="product_list">Spectrum slot: Product List — fill in Studio</div>
{%- endif -%}
{%- comment -%} Spectrum slot "Product List" (data-spectrum-slot="product_list") — fill via Studio with a catalog snippet whose placement.slotKey is "product_list". Variations resolve from the live catalog, not from a list baked here. {%- endcomment -%}
{%- comment -%}SPECTRUM_PLACEHOLDER:END{%- endcomment -%}
{% schema %}
{
  "name": "Product List",
  "presets": [
    {
      "name": "Product List"
    }
  ]
}
{% endschema %}
