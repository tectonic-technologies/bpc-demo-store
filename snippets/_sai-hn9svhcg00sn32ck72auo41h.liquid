{%- comment -%}AUTO-GENERATED BY SPECTRUM STUDIO. DO NOT MODIFY.{%- endcomment -%}

{%- if sai_state != 'draft' and sai_state != 'live' -%}
  <!-- SAI:WRAPPER:STATE_DEFAULT input={{ sai_state | default: 'NIL' }} fell_back_to=live -->
  {%- assign sai_state = 'live' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_vis = 'on' -%}
{%- else -%}
  {%- assign sai_vis = 'off' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_handle = 'spectrum_snippet_vp9q4kxn_hn9svhcg00sn32ck72auo41h_draft' -%}
{%- else -%}
  {%- assign sai_handle = 'spectrum_snippet_vp9q4kxn_hn9svhcg00sn32ck72auo41h_live' -%}
{%- endif -%}

{%- assign sai_mo = metaobjects.spectrum_snippet_instance[sai_handle] -%}
{%- unless sai_mo -%}
  {%- paginate metaobjects.spectrum_snippet_instance.values by 250 -%}
    {%- for sai_entry in metaobjects.spectrum_snippet_instance.values -%}
      {%- if sai_entry.system.handle == sai_handle -%}
        {%- assign sai_mo = sai_entry -%}
        {%- break -%}
      {%- endif -%}
    {%- endfor -%}
  {%- endpaginate -%}
{%- endunless -%}

{%- if sai_mo and sai_mo.meta.value.status == 'active' -%}
  {%- assign sai_default = nil -%}
  {%- for sai_v in sai_mo.variants.value.variants -%}
    {%- if sai_v.isDefault -%}
      {%- assign sai_default = sai_v -%}
      {%- break -%}
    {%- endif -%}
  {%- endfor -%}

  {%- if sai_default -%}
    {%- capture sai_envelope -%}
    {
    "v":1,
    "instanceId":"hn9svhcg00sn32ck72auo41h",
    "snippetId":"vp9q4kxn",
    "experienceId":"019e9e78-1bf3-7dd1-a9a5-692d54282488"
    ,"variants":{{ sai_mo.variants.value.variants | json }}
    ,"pools":{
    "images":{
    {%- for sai_item in sai_mo.images.value -%}
      {%- unless forloop.first -%},{%- endunless -%}
      "{{ sai_item.system.handle | default: sai_item.id }}":{
        "url":{{ sai_item | image_url: width: 2400 | json }}
      }
    {%- endfor -%}
    }
    }
    }
    {%- endcapture -%}

    <style data-spectrum-style="hn9svhcg00sn32ck72auo41h">
    {%- for sai_v in sai_mo.variants.value.variants -%}
      {%- if sai_v.css != blank -%}{{ sai_v.css }}{%- endif -%}
    {%- endfor -%}
    </style>
    <div data-spectrum-instance-id="hn9svhcg00sn32ck72auo41h" data-spectrum-snippet-id="vp9q4kxn" data-spectrum-variant-id="{{ sai_default.variantId }}" data-spectrum-vis="{{ sai_vis }}" data-spectrum-lq-snippet="mut-hn9svhcg00sn32ck72auo41h" data-spectrum-experience="pg1" data-spectrum-feature-slug="fomo">
      <script type="application/json" data-spectrum-envelope>{{ sai_envelope | replace: '</script>', '<\/script>' }}</script>
      {% render '_sai-snippet-vp9q4kxn', variant: sai_default, mo: sai_mo, mutation_handle: 'mut-hn9svhcg00sn32ck72auo41h', experience_handle: 'pg1' %}
    </div>
  {%- endif -%}
{%- endif -%}