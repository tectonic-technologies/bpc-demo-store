{%- comment -%}AUTO-GENERATED BY SPECTRUM STUDIO. DO NOT MODIFY.{%- endcomment -%}

{%- if sai_state != 'draft' and sai_state != 'live' -%}
  <!-- SAI:WRAPPER:STATE_DEFAULT input={{ sai_state | default: 'NIL' }} fell_back_to=live -->
  {%- assign sai_state = 'live' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_vis = 'on' -%}
{%- else -%}
  {%- assign sai_vis = 'off' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_handle = 'spectrum_snippet_d1odzamh_ii3yxg7gol27w9b88jrv8un5_draft' -%}
{%- else -%}
  {%- assign sai_handle = 'spectrum_snippet_d1odzamh_ii3yxg7gol27w9b88jrv8un5_live' -%}
{%- endif -%}

{%- assign sai_mo = metaobjects.spectrum_snippet_instance[sai_handle] -%}
{%- unless sai_mo -%}
  {%- paginate metaobjects.spectrum_snippet_instance.values by 250 -%}
    {%- for sai_entry in metaobjects.spectrum_snippet_instance.values -%}
      {%- if sai_entry.system.handle == sai_handle -%}
        {%- assign sai_mo = sai_entry -%}
        {%- break -%}
      {%- endif -%}
    {%- endfor -%}
  {%- endpaginate -%}
{%- endunless -%}

{%- if sai_mo and sai_mo.meta.value.status == 'active' -%}
  {%- assign sai_default = nil -%}
  {%- for sai_v in sai_mo.variants.value.variants -%}
    {%- if sai_v.isDefault -%}
      {%- assign sai_default = sai_v -%}
      {%- break -%}
    {%- endif -%}
  {%- endfor -%}

  {%- if sai_default -%}
    {%- capture sai_envelope -%}
    {
    "v":1,
    "instanceId":"ii3yxg7gol27w9b88jrv8un5",
    "snippetId":"d1odzamh",
    "experienceId":"019e9e78-1bf3-7dd1-a9a5-692d54282488"
    ,"variants":{{ sai_mo.variants.value.variants | json }}
    ,"pools":{
    }
    }
    {%- endcapture -%}

    <style data-spectrum-style="ii3yxg7gol27w9b88jrv8un5">
    {%- for sai_v in sai_mo.variants.value.variants -%}
      {%- if sai_v.css != blank -%}{{ sai_v.css }}{%- endif -%}
    {%- endfor -%}
    </style>
    <div data-spectrum-instance-id="ii3yxg7gol27w9b88jrv8un5" data-spectrum-snippet-id="d1odzamh" data-spectrum-variant-id="{{ sai_default.variantId }}" data-spectrum-vis="{{ sai_vis }}" data-spectrum-lq-snippet="mut-ii3yxg7gol27w9b88jrv8un5" data-spectrum-experience="pg1" data-spectrum-feature-slug="reviews">
      <script type="application/json" data-spectrum-envelope>{{ sai_envelope | replace: '</script>', '<\/script>' }}</script>
      {% render '_sai-snippet-d1odzamh', variant: sai_default, mo: sai_mo, mutation_handle: 'mut-ii3yxg7gol27w9b88jrv8un5', experience_handle: 'pg1' %}
    </div>
  {%- endif -%}
{%- endif -%}