{%- comment -%}AUTO-GENERATED BY SPECTRUM STUDIO. DO NOT MODIFY.{%- endcomment -%}

{%- if sai_state != 'draft' and sai_state != 'live' -%}
  <!-- SAI:WRAPPER:STATE_DEFAULT input={{ sai_state | default: 'NIL' }} fell_back_to=live -->
  {%- assign sai_state = 'live' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_vis = 'on' -%}
{%- else -%}
  {%- assign sai_vis = 'off' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_handle = 'spectrum_snippet_ur0h3zgp_o4bcadk3nvw3b90oxkzlh2c4_draft' -%}
{%- else -%}
  {%- assign sai_handle = 'spectrum_snippet_ur0h3zgp_o4bcadk3nvw3b90oxkzlh2c4_live' -%}
{%- endif -%}

{%- assign sai_mo = metaobjects.spectrum_snippet_instance[sai_handle] -%}
{%- unless sai_mo -%}
  {%- paginate metaobjects.spectrum_snippet_instance.values by 250 -%}
    {%- for sai_entry in metaobjects.spectrum_snippet_instance.values -%}
      {%- if sai_entry.system.handle == sai_handle -%}
        {%- assign sai_mo = sai_entry -%}
        {%- break -%}
      {%- endif -%}
    {%- endfor -%}
  {%- endpaginate -%}
{%- endunless -%}

{%- if sai_mo and sai_mo.meta.value.status == 'active' -%}
  {%- assign sai_default = nil -%}
  {%- for sai_v in sai_mo.variants.value.variants -%}
    {%- if sai_v.isDefault -%}
      {%- assign sai_default = sai_v -%}
      {%- break -%}
    {%- endif -%}
  {%- endfor -%}

  {%- if sai_default -%}
    {%- capture sai_envelope -%}
    {
    "v":1,
    "instanceId":"o4bcadk3nvw3b90oxkzlh2c4",
    "snippetId":"ur0h3zgp",
    "experienceId":"019e9789-21e3-73c6-9696-9794758cdd5b"
    ,"variants":{{ sai_mo.variants.value.variants | json }}
    ,"pools":{
    "images":{
    {%- for sai_item in sai_mo.images.value -%}
      {%- unless forloop.first -%},{%- endunless -%}
      "{{ sai_item.system.handle | default: sai_item.id }}":{
        "url":{{ sai_item | image_url: width: 2400 | json }}
      }
    {%- endfor -%}
    }
    }
    }
    {%- endcapture -%}

    <style data-spectrum-style="o4bcadk3nvw3b90oxkzlh2c4">
    {%- for sai_v in sai_mo.variants.value.variants -%}
      {%- if sai_v.css != blank -%}{{ sai_v.css }}{%- endif -%}
    {%- endfor -%}
    </style>
    <div data-spectrum-instance-id="o4bcadk3nvw3b90oxkzlh2c4" data-spectrum-snippet-id="ur0h3zgp" data-spectrum-variant-id="{{ sai_default.variantId }}" data-spectrum-vis="{{ sai_vis }}" data-spectrum-lq-snippet="mut-o4bcadk3nvw3b90oxkzlh2c4" data-spectrum-experience="_system-155719762098" data-spectrum-feature-slug="fomo">
      <script type="application/json" data-spectrum-envelope>{{ sai_envelope | replace: '</script>', '<\/script>' }}</script>
      {% render '_sai-snippet-ur0h3zgp', variant: sai_default, mo: sai_mo, mutation_handle: 'mut-o4bcadk3nvw3b90oxkzlh2c4', experience_handle: '_system-155719762098' %}
    </div>
  {%- endif -%}
{%- endif -%}