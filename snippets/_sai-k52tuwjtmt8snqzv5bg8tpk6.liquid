{%- comment -%}AUTO-GENERATED BY SPECTRUM STUDIO. DO NOT MODIFY.{%- endcomment -%}

{%- if sai_state != 'draft' and sai_state != 'live' -%}
  <!-- SAI:WRAPPER:STATE_DEFAULT input={{ sai_state | default: 'NIL' }} fell_back_to=live -->
  {%- assign sai_state = 'live' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_vis = 'on' -%}
{%- else -%}
  {%- assign sai_vis = 'off' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_handle = 'spectrum_snippet_o1uear1c_k52tuwjtmt8snqzv5bg8tpk6_draft' -%}
{%- else -%}
  {%- assign sai_handle = 'spectrum_snippet_o1uear1c_k52tuwjtmt8snqzv5bg8tpk6_live' -%}
{%- endif -%}

{%- assign sai_mo = metaobjects.spectrum_snippet_instance[sai_handle] -%}
{%- unless sai_mo -%}
  {%- paginate metaobjects.spectrum_snippet_instance.values by 250 -%}
    {%- for sai_entry in metaobjects.spectrum_snippet_instance.values -%}
      {%- if sai_entry.system.handle == sai_handle -%}
        {%- assign sai_mo = sai_entry -%}
        {%- break -%}
      {%- endif -%}
    {%- endfor -%}
  {%- endpaginate -%}
{%- endunless -%}

{%- if sai_mo and sai_mo.meta.value.status == 'active' -%}
  {%- assign sai_default = nil -%}
  {%- for sai_v in sai_mo.variants.value.variants -%}
    {%- if sai_v.isDefault -%}
      {%- assign sai_default = sai_v -%}
      {%- break -%}
    {%- endif -%}
  {%- endfor -%}

  {%- if sai_default -%}
    {%- capture sai_envelope -%}
    {
    "v":1,
    "instanceId":"k52tuwjtmt8snqzv5bg8tpk6",
    "snippetId":"o1uear1c",
    "experienceId":"019e9789-21e3-73c6-9696-9794758cdd5b"
    ,"variants":{{ sai_mo.variants.value.variants | json }}
    ,"pools":{
    }
    }
    {%- endcapture -%}

    {%- assign sai_section_width = sai_default.content.section_width -%}
    <style data-spectrum-style="k52tuwjtmt8snqzv5bg8tpk6">
    {%- for sai_v in sai_mo.variants.value.variants -%}
      {%- if sai_v.css != blank -%}{{ sai_v.css }}{%- endif -%}
    {%- endfor -%}
    </style>
    <div data-spectrum-instance-id="k52tuwjtmt8snqzv5bg8tpk6" data-spectrum-snippet-id="o1uear1c" data-spectrum-variant-id="{{ sai_default.variantId }}" data-spectrum-vis="{{ sai_vis }}" data-spectrum-section-width="{{ sai_section_width }}" data-spectrum-lq-snippet="mut-k52tuwjtmt8snqzv5bg8tpk6" data-spectrum-experience="_system-155719762098" data-spectrum-feature-slug="virtual_try_on">
      <script type="application/json" data-spectrum-envelope>{{ sai_envelope | replace: '</script>', '<\/script>' }}</script>
      {% render '_sai-snippet-o1uear1c', variant: sai_default, mo: sai_mo, mutation_handle: 'mut-k52tuwjtmt8snqzv5bg8tpk6', experience_handle: '_system-155719762098' %}
    </div>
  {%- endif -%}
{%- elsif sai_state == 'draft' -%}
  <div data-spectrum-instance-id="k52tuwjtmt8snqzv5bg8tpk6" data-spectrum-snippet-id="o1uear1c" data-spectrum-vis="{{ sai_vis }}" data-spectrum-lq-snippet="mut-k52tuwjtmt8snqzv5bg8tpk6" data-spectrum-experience="_system-155719762098" data-spectrum-feature-slug="virtual_try_on">
    <!-- SAI:WRAPPER:EMPTY_DRAFT_MO_MISSING snippetId=o1uear1c instanceId=k52tuwjtmt8snqzv5bg8tpk6 -->
  </div>
{%- endif -%}