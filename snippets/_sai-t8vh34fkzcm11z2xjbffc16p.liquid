{%- comment -%}AUTO-GENERATED BY SPECTRUM STUDIO. DO NOT MODIFY.{%- endcomment -%}

{%- if sai_state != 'draft' and sai_state != 'live' -%}
  <!-- SAI:WRAPPER:STATE_DEFAULT input={{ sai_state | default: 'NIL' }} fell_back_to=live -->
  {%- assign sai_state = 'live' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_vis = 'on' -%}
{%- else -%}
  {%- assign sai_vis = 'off' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_handle = 'spectrum_snippet_kvb3hsy7_t8vh34fkzcm11z2xjbffc16p_draft' -%}
{%- else -%}
  {%- assign sai_handle = 'spectrum_snippet_kvb3hsy7_t8vh34fkzcm11z2xjbffc16p_live' -%}
{%- endif -%}

{%- assign sai_mo = metaobjects.spectrum_snippet_instance[sai_handle] -%}
{%- unless sai_mo -%}
  {%- paginate metaobjects.spectrum_snippet_instance.values by 250 -%}
    {%- for sai_entry in metaobjects.spectrum_snippet_instance.values -%}
      {%- if sai_entry.system.handle == sai_handle -%}
        {%- assign sai_mo = sai_entry -%}
        {%- break -%}
      {%- endif -%}
    {%- endfor -%}
  {%- endpaginate -%}
{%- endunless -%}

{%- if sai_mo and sai_mo.meta.value.status == 'active' -%}
  {%- assign sai_default = nil -%}
  {%- for sai_v in sai_mo.variants.value.variants -%}
    {%- if sai_v.isDefault -%}
      {%- assign sai_default = sai_v -%}
      {%- break -%}
    {%- endif -%}
  {%- endfor -%}

  {%- if sai_default -%}
    {%- capture sai_envelope -%}
    {
    "v":1,
    "instanceId":"t8vh34fkzcm11z2xjbffc16p",
    "snippetId":"kvb3hsy7",
    "experienceId":"019ea332-17f8-7f4a-90c1-efb7ba21b3f4"
    ,"variants":{{ sai_mo.variants.value.variants | json }}
    ,"pools":{
    "metaobjects":{
    {%- for sai_item in sai_mo.metaobjects.value -%}
      {%- unless forloop.first -%},{%- endunless -%}
      "{{ sai_item.system.handle | default: sai_item.id }}":{
        "system.handle":{{ sai_item.system.handle | json }}
      }
    {%- endfor -%}
    }
    ,
    "shoppable_videos":{
    {%- for sai_item in sai_mo.shoppable_videos.value -%}
      {%- unless forloop.first -%},{%- endunless -%}
      "{{ sai_item.system.handle | default: sai_item.id }}":{
        "title":{{ sai_item.title | json }},
        "assets":{{ sai_item.assets | json }},
        "product_tags":{{ sai_item.product_tags | json }}
      }
    {%- endfor -%}
    }
    }
    }
    {%- endcapture -%}

    <style data-spectrum-style="t8vh34fkzcm11z2xjbffc16p">
    {%- for sai_v in sai_mo.variants.value.variants -%}
      {%- if sai_v.css != blank -%}{{ sai_v.css }}{%- endif -%}
    {%- endfor -%}
    </style>
    <div data-spectrum-instance-id="t8vh34fkzcm11z2xjbffc16p" data-spectrum-snippet-id="kvb3hsy7" data-spectrum-variant-id="{{ sai_default.variantId }}" data-spectrum-vis="{{ sai_vis }}" data-spectrum-lq-snippet="mut-t8vh34fkzcm11z2xjbffc16p" data-spectrum-experience="pg2" data-spectrum-feature-slug="shoppable_videos">
      <script type="application/json" data-spectrum-envelope>{{ sai_envelope | replace: '</script>', '<\/script>' }}</script>
      {% render '_sai-snippet-kvb3hsy7', variant: sai_default, mo: sai_mo, mutation_handle: 'mut-t8vh34fkzcm11z2xjbffc16p', experience_handle: 'pg2' %}
    </div>
  {%- endif -%}
{%- endif -%}